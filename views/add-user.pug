doctype html
html(lang='en')

  head
    meta(charset='utf-8')
    meta(http-equiv='X-UA-Compatible', content='IE=edge')
    meta(name='viewport', content='width=device-width, initial-scale=1, shrink-to-fit=no')
    meta(name='description', content='')
    meta(name='author', content='')

    script.
      (function() {
        'use strict';
        window.addEventListener('load', function() {
          var forms = document.getElementsByClassName('needs-validation');
          var validation = Array.prototype.filter.call(forms, function(form) {
            form.addEventListener('submit', function(event) {
              if (form.checkValidity() === false) {
                event.preventDefault();
                event.stopPropagation();
              }
              form.classList.add('was-validated');
            }, false);
          });
        }, false);
      })();

    title Student Database

    include includes/css/core.pug

    include includes/css/custom.pug

  body#page-top.fixed-nav.sticky-footer.bg-dark
    include includes/navbar.pug
    .content-wrapper
      .container-fluid
        // Breadcrumbs
        ol.breadcrumb
          li.breadcrumb-item
            a(href='/') Dashboard
          li.breadcrumb-item.active Input New User
        .card.mb-3
          .card-header
            i.fa.fa-check-square-o
            |  Input New User
          .card-body
            form.needs-validation(action='/add-user', method='POST', novalidate='')
              .row.form-group
                .col-4
                  label(for='email') Email:
                .col-6
                  input.form-control(type='email', name='email', placeholder='Input Email', required='')
                  .invalid-feedback
                    | Please enter a valid email
              .row.form-group
                .col-4
                  label(for='username') Username:
                .col-6
                  input.form-control(type='text', name='username', placeholder='Input Username', required='')
                  .invalid-feedback
                    | Please enter your username
              .row.form-group
                .col-4
                  label(for='password') Password:
                .col-6
                  input.form-control(type='password', name='password', placeholder='Input Username', required='')
                  .invalid-feedback
                    | Please enter your password
              input.btn.btn-primary(type='submit', value='Submit')
    
      include includes/footer.pug

      include includes/scroll-to-top.pug

      include includes/js/core.pug

      include includes/js/custom.pug
    
    
